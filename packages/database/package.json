{
  "name": "@mumtazai/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared Prisma schema & client for Mumtaz AI (frontend + backend)",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./client": "./index.js"
  },
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
