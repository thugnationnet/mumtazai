# Deploy configuration — copy to .deploy.env and fill in real values
# .deploy.env is gitignored; NEVER commit credentials
SSH_KEY_PATH=mumtazai-prod.pem
DEPLOY_SERVER=ubuntu@13.228.143.252
DEPLOY_REMOTE_DIR=/home/ubuntu/shiny-friend-disco
