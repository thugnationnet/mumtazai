# ============================================
# BEN-SEGA — Dedicated Agent Backend
# Copy this to .env and fill in real values
# ============================================

# Agent identity (do not change)
AGENT_ID=ben-sega
PORT=4002

# Per-agent rate limit (messages per IP per hour)
AGENT_RATE_LIMIT_PER_IP=30

# Provider cascade — MISTRAL is PRIMARY for Ben Sega
AGENT_PROVIDER_CASCADE=mistral,openai,xai
AGENT_PRIMARY_PROVIDER=mistral

# Per-mode dedicated provider order
# chat: mistral primary (retro gaming persona — fast & fun)
AGENT_PROVIDER_CHAT=mistral,openai,xai
# code: mistral codestral purpose-built for code
AGENT_PROVIDER_CODE=mistral,openai,xai
# images: DALL-E (openai) must be primary for generate_image tool
AGENT_PROVIDER_IMAGES=openai,mistral,xai
# search: xAI Grok best for real-time web synthesis
AGENT_PROVIDER_SEARCH=xai,mistral,openai
# video: openai LLM calls generate_video tool → RunwayML does the actual generation
AGENT_PROVIDER_VIDEO=openai,mistral,xai

# Max tokens per response
AGENT_MAX_TOKENS=16384

# Max tool execution rounds
AGENT_MAX_TOOL_ROUNDS=3

# ===== AI Provider Keys =====
OPENAI_API_KEY=
OPENAI_API_KEY_BACKUP=
XAI_API_KEY=
MISTRAL_API_KEY=

# ===== RunwayML — Video Generation =====
# IMPORTANT: Must also be set in ecosystem.config.cjs env section
# because video-generation-service.js reads it at module load time
RUNWAY_API_KEY=

# ===== Database =====
UNIVERSAL_CHAT_DATABASE_URL=

# ===== Auth =====
JWT_SECRET=
SESSION_SECRET=

# ===== CORS =====
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3100,https://mumtaz.ai,https://www.mumtaz.ai,https://chat.mumtaz.ai,https://ben-sega.mumtaz.ai
